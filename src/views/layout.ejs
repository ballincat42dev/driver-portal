<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof title !== 'undefined' ? title : 'Driver Portal' %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
  <style>
    main { max-width: 960px; margin: 2rem auto; }
    header, footer { max-width: 960px; margin: 1rem auto; }
    .error { color: #b00020; }
    nav ul { display: flex; gap: 1rem; list-style: none; padding: 0; }
  </style>
  </head>
<body>
  <header>
    <nav>
      <ul>
        <% if (user) { %>
          <li><a href="/dashboard">Dashboard</a></li>
          <li><a href="/submit">Submit</a></li>
          <% if (user.role === 'admin') { %>
            <li><a href="/admin">Admin</a></li>
          <% } %>
          <li>
            <form method="post" action="/logout">
              <button type="submit" class="secondary">Logout</button>
            </form>
          </li>
        <% } else { %>
          <li><a href="/login">Login</a></li>
          <li><a href="/register">Register</a></li>
        <% } %>
      </ul>
    </nav>
  </header>
  <main>
    <%- body %>
  </main>
  <footer>
    <small>&copy; <%= new Date().getFullYear() %> Driver Portal</small>
  </footer>
</body>
</html>


